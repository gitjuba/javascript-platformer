const CANVAS_WIDTH = 1280;
const CANVAS_HEIGHT = 720;

const GRID_SIZE = 10;
const PLAYER_W = 2 * GRID_SIZE;
const PLAYER_H = 2 * GRID_SIZE;

const Objects = {
  NONE: 0,
  PLAYER: 1,
  GOAL: 2,
  OBSTACLE: 3
};

const GameStates = {
  SPLASH: 0,
  GAME: 1,
  EDITOR: 2
};
